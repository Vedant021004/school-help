{
  "name": "ai-teacher-assistant-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "test": "pytest backend/test_backend.py"
  }
}
